/* Autogenerated with kurento-module-creator */

#include "ElementConnected.hpp"
#include <jsonrpc/JsonSerializer.hpp>
#include "MediaElement.hpp"
#include "MediaType.hpp"
#include <chrono>
#include <ctime>
#include <string>

namespace kurento
{

void
ElementConnected::Serialize (JsonSerializer &s)
{
  Media::Serialize (s);

  s.SerializeNVP (sink);
  s.SerializeNVP (mediaType);
  s.SerializeNVP (sourceMediaDescription);
  s.SerializeNVP (sinkMediaDescription);
}

} /* kurento */

namespace kurento
{

void Serialize (std::shared_ptr<kurento::ElementConnected> &event, JsonSerializer &s)
{
  if (!s.IsWriter && !event) {
    event.reset (new kurento::ElementConnected() );
  }

  if (event) {
    event->Serialize (s);
  }
}

} /* kurento */
