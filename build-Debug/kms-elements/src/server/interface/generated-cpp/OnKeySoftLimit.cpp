/* Autogenerated with kurento-module-creator */

#include "OnKeySoftLimit.hpp"
#include <jsonrpc/JsonSerializer.hpp>
#include <chrono>
#include <ctime>
#include <string>

namespace kurento
{

void
OnKeySoftLimit::Serialize (JsonSerializer &s)
{
  Media::Serialize (s);

  s.SerializeNVP (mediaType);
}

} /* kurento */

namespace kurento
{

void Serialize (std::shared_ptr<kurento::OnKeySoftLimit> &event, JsonSerializer &s)
{
  if (!s.IsWriter && !event) {
    event.reset (new kurento::OnKeySoftLimit() );
  }

  if (event) {
    event->Serialize (s);
  }
}

} /* kurento */
