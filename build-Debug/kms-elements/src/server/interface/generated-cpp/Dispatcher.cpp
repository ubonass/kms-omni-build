/* Autogenerated with kurento-module-creator */

#include "Dispatcher.hpp"
#include <mutex>

namespace kurento
{

static std::vector<std::string> hierarchyDispatcher;
std::once_flag initFlagDispatcher;

static void
initHierarchy ()
{
  hierarchyDispatcher.emplace_back("kurento.Hub");
  hierarchyDispatcher.emplace_back("kurento.MediaObject");
}

const std::vector<std::string> &
Dispatcher::getHierarchy () const
{
  std::call_once (initFlagDispatcher,initHierarchy);
  return hierarchyDispatcher;
}

const std::string&
Dispatcher::getType () const {
  static std::string typeName = "Dispatcher";

  return typeName;
}

const std::string&
Dispatcher::getQualifiedType () const {
  static std::string qualifiedTypeName = getModule() + "." + getType();

  return qualifiedTypeName;
}

const std::string&
Dispatcher::getModule () const {
  static std::string moduleName =  "kurento";

  return moduleName;
}

} /* kurento */
